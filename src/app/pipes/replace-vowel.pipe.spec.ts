import { ReplaceVowelPipe } from './replace-vowel.pipe';

describe('ReplaceVowelPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceVowelPipe();
    expect(pipe).toBeTruthy();
  });
});
